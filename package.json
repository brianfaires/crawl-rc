{
  "name": "crawl-rc",
  "version": "1.3.1",
  "description": "Crawl RC configuration files",
  "scripts": {
    "prepare": "git config core.hooksPath hooks",
    "test": "./tests/run.sh",
    "lint": "luacheck --formatter plain lua/",
    "lint:fix": "luacheck --formatter plain --fix lua/",
    "lint:quiet": "luacheck --formatter plain --quiet lua/"
  },
  "devDependencies": {},
  "keywords": [
    "lua",
    "crawl",
    "rc"
  ],
  "author": "buehler",
  "license": "MIT"
}
